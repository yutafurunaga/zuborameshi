<!DOCTYPE html>
<html>
  <head>
    <title>zuborameshi</title>
     <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="d-flex flex-column vh-100">
    <%= render 'layouts/header' %>
     <%#= form.text_field :tag %>

     <h3>レシピ検索はこちら</h3>
     <%= render 'public/searches/search' %>
     <div class="container">
     <%= form_with(url: recipes_path, method: :get, class: "form-inline mb-3") do |form| %>
     <div class="form-group mr-2">
      <%= form.label :tag, 'タグ検索:', class: "mr-2" %>
      <%= select_tag :tag, options_for_select(ActsAsTaggableOn::Tag.all { |tag| [tag.name, tag.name] }), include_blank: true, class: 'form-control' %>
     </div>
      <%= form.submit '検索', class: 'btn btn-primary' %>
     <% end %>
    </div>

    <!--<＃= form_tag(search_path, method: :get) do %>-->
      <!--<div class="form-group">-->
        <%#= label_tag :tag, 'タグ検索:' %>
        <%#= select_tag :tag, options_for_select(ActsAsTaggableOn::Tag.all { |tag| [tag.name, tag.name] }), include_blank: true, class: 'form-control' %>
      <!--</div>-->
    <!--   <div class="form-group">-->
    <!--      <%#= label_tag :keyword, 'キーワード検索:' %>-->
    <!--      <%#= text_field_tag :keyword, params[:keyword], class: 'form-control' %>-->
    <!--       </div>-->
    <!--      <%#= submit_tag '検索', class: 'btn btn-primary' %>-->
    <!--  <%# end %>-->
    <main class="mb-auto">
    <%= yield %>
    </main>
    <%= render 'layouts/footer' %>
  </body>
</html>
