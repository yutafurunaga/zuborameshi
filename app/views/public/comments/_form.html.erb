<div class="comments-section">
  <h3>コメント</h3>
  <%= form_with(model: [recipe, Comment.new], local: false) do |form| %>
　<%= form.text_area :comment, rows:'5',placeholder: "コメントをここに" %>
    <%= form.submit "コメントする", class: "btn btn-primary" %>
  <% end %>
</div>